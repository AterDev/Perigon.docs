# 多租户

多租户也是常见的系统设计需求，为了满足这个需求，我们也进行了良好的设计。


想象以下场景：

我有1000+个普通租户，每个租户的数据量不是很大，没有强资源隔离，属于成本敏感型客户；

我有10+个大客户租户，每个租户的数据量非常大，且有资源隔离的需求，属于高端客户，以稳定为首要目标；


## 默认多租户的支持

默认采用"一个数据库，多租户"的设计方式，通过在实体中添加`TenantId`属性来区分不同的租户数据。这适用于大部分场景，即每个租户的数据量不是很大，也没有资源隔离的需求。

## 独立租户

对于有资源隔离需求的场景，或某个租户数据量特别大时，
